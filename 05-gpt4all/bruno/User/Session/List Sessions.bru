meta {
  name: List Sessions
  type: http
  seq: 1
}

get {
  url: {{API_URL}}/api/user/session
  body: none
  auth: bearer
}

headers {
  device_id: {{DEVICE_ID}}
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}
