meta {
  name: Delete Friend
  type: http
  seq: 4
}

delete {
  url: {{API_URL}}/api/friend/{{FRIEND_ID}}
  body: none
  auth: bearer
}

headers {
  device_id: {{DEVICE_ID}}
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}
